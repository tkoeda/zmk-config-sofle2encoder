/ {
    /* Override just the triggers-per-rotation on the existing sensors node */
    sensors: sensors {
        triggers-per-rotation = <30>;
    };
};

/* Also enable each encoder and (optionally) override its own steps */
&left_encoder {
    status = "okay";
    /* If you prefer per-encoder steps instead of global triggers-per-rotation:
       steps = <30>;  
    */
};

&right_encoder {
    status = "okay";
    /* steps = <30>; */
};
